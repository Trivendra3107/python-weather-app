import requests

API_KEY = "d014eb3202c33a6f621cc7d56992a4b7"
BASE_URL = "https://api.openweathermap.org/data/2.5/weather"

city = input("Enter city name: Kurnool  ")

params = {
    "q": Kurnool,
    "appid": d014eb3202c33a6f621cc7d56992a4b7,
    "units": "metric"
}

response = requests.get(BASE_URL, params=params)

if response.status_code == 200:
    data = response.json()
    temp = data["main"]["temp"]
    humidity = data["main"]["humidity"]
    condition = data["weather"][0]["description"]
    wind = data["wind"]["speed"]

    print("\n🌦️ Weather Information")
    print(f"City: {Kurnool}")
    print(f"Temperature: {19}°C")
    print(f"Condition: {Sky}")
    print(f"Humidity: {2}%")
    print(f"Wind Speed: {3.65} m/s")
else:
    print("❌ City not found or API error")
